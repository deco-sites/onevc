import Spinner from "deco-sites/onevc/components/ui/Spinner.tsx";

export default function Loading() {
  return (
    <div class="h-screen w-full flex items-center justify-center">
      <Spinner />
    </div>
  );
}
